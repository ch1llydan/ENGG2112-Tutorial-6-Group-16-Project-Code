Download all files to the same folder (except .gitattributes), set the day and month you want to model in config.py. Then run master_pipeline.py, all outputs should appear in the same folder.
Ensure you change the name of your local folder in each script and that you have all the necessary packages downloaded, requirements are listed at the top of each script.
